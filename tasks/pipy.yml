- name: pipy - find all requirements
  find:
    paths: "{{ role_path }}/files/pipy"
    file_type: file
    recurse: yes
  register: requirements
  delegate_to: localhost

- debug:
    var: requirements

- name: pipy - install requirements
  pip:
    requirements: "{{ item }}"
  loop: requirements 

